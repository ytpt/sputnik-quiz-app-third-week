import styled from "styled-components";

export const Wrapper = styled.div` 
    label {
        display: flex; 
        flex-direction: row;
        align-items: center;
        column-gap: 5px;
    }
    
    p {
        margin: 0;
    }
`;